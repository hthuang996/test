use std::net::{TcpListener, TcpStream};
use std::io::{
    Read,
    Write,
};

use message::

fn main() {
    let _stream = TcpStream::connect("localhost:3000").unwrap();

    let b = .to_be_bytes
    _stream.write(i);
}
